# ─────────────────────────────────────────────────────────────
# AI Multi-Agent Platform — Backend dependencies
# Aligns with TECH_STACK_DECISION.md
# ─────────────────────────────────────────────────────────────

# --- Web framework / ASGI ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # multipart/form-data file uploads (FR-003)

# --- Configuration / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0            # pydantic EmailStr validation

# --- Relational database (PostgreSQL 16, async) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Cache / sessions (Redis 7) ---
redis==5.2.1

# --- Vector database (Qdrant) ---
qdrant-client==1.12.2

# --- Object storage (MinIO / S3) ---
minio==7.2.13

# --- Event bus (Kafka) ---
aiokafka==0.12.0

# --- Auth / security (OIDC/JWT, hashing) ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# --- HTTP client (LLM / embeddings / external sources) ---
httpx==0.28.1
tenacity==9.0.0                   # retry policy (Event Bus retry semantics)

# --- Observability ---
structlog==24.4.0

# --- Document processing (FR-004 / FR-005) ---
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5

# --- OCR (FR-006): needs system `tesseract-ocr` + `poppler-utils` installed ---
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.0.0

# --- Metadata extraction (FR-007) ---
langdetect==1.0.9

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
